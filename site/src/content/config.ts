// Content collections. The old per-metric "reads" collection (markdown at
// src/content/reads) was retired on 2026-06-22, replaced by the bespoke
// short-form reads at /economy/reads/* (registry: src/lib/reads-index.ts).
// The stats.json freshness pipeline (build-stats / check-stats / metric-slugs)
// that backed those reads was retired with them.
export const collections = {};
