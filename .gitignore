# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
env/
*.egg-info/

# --- Node / site build ---
node_modules/
site/dist/
site/.astro/
# wrangler's local cache (can hold geolocation data from `wrangler dev`)
.wrangler/

# --- Build-time database data ---
infra/db/timescale_data/
infra/db/data/

# --- Source data & ETL intermediates (not committed — see CONTRIBUTING) ---
# Raw RBI/NPCI downloads and every generated/fetched ETL artifact stay local.
/data/
etl/**/*.json
etl/**/*.csv
# product-dict.json is committed config, not a generated artifact — keep it tracked.
!etl/rbi/product-dict.json
# Generated static datasets the site emits at build time.
site/public/data/
*.log

# --- Environment / secrets ---
.env
*.env
.env.*
.env.local
.env.*.local

# --- Local-only: internal tooling, planning docs, agent config (not public deliverables) ---
# Internal docs moved to a separate private repo (2026-07-09); the ignore
# stays as a safety net so a recreated docs/ can't be committed by accident.
# pipeline.md is the one deliberate exception: the public source-to-page
# lineage + privacy disclosure for the game beacon and /meta (README links it).
docs/*
!docs/pipeline.md
# EDITORIAL.md: maintainer-authored editorial standard, held back from the public
# repo pending review — kept local-only so it can't be committed by accident.
EDITORIAL.md
_bmad/
_bmad-output/
panels/
notes.md
TODO.md
.claude/
opencode.json
**/.od-skills/
# Legacy/internal ops (Grafana dashboards, nginx, image renderer) kept out of the public repo.
infra/grafana/
infra/grafana_custom/
infra/dashboard/
infra/e-charts/
infra/nginx/
infra/timescaledb/

# --- Misc ---
.DS_Store
Thumbs.db
tsoi-shots/

# Wrangler local secrets — never commit (mirrors the .env rules above)
.dev.vars
.dev.vars.*
